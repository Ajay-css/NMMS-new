# Production Environment Variables
# Copy these to Render environment variables

NODE_ENV=production
PORT=10000

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret - Use a strong random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters

# Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend URL (comma-separated for multiple origins)
# IMPORTANT: Set this to your Vercel frontend URL
# Example: https://xaviers-nmms-scanner.vercel.app,https://www.your-domain.com


